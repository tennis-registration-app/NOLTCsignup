import { readFileSync as R, writeFileSync as W } from "fs";
const B = "src";
function fix(p, fn) { W(p, fn(R(p, "utf8"))); console.log("Fixed:", p.split("/src/")[1]||p.split("/").pop()); }

// Batch 1: Simple hooks and platform utilities
fix(B+"/registration/hooks/useDebounce.ts", c => c.replace("export const useDebounce = (value, delay) => {", "export const useDebounce = <T>(value: T, delay: number): T => {"));
fix(B+"/platform/prefsStorage.ts", c => c.replace("export function getPref(key) {", "export function getPref(key: string) {").replace("export function getCache(key) {", "export function getCache(key: string) {").replace("export function setCache(key, value) {", "export function setCache(key: string, value: unknown) {"));
fix(B+"/platform/registerGlobals.ts", c => c.replace("export function setTennisGlobal(tennis) {", "export function setTennisGlobal(tennis: Record<string, unknown>) {").replace("export function setGeolocationServiceGlobal(service) {", "export function setGeolocationServiceGlobal(service: unknown) {").replace("export function setNoltcUseApiGlobal(value) {", "export function setNoltcUseApiGlobal(value: boolean) {").replace("export function setLoadDataGlobal(fn) {", "export function setLoadDataGlobal(fn: () => void | Promise<void>) {").replace("export function setRefreshBoardGlobal(fn) {", "export function setRefreshBoardGlobal(fn: () => void) {").replace("export function setRefreshAdminViewGlobal(fn) {", "export function setRefreshAdminViewGlobal(fn: () => void) {").replace("export function setAdminRefreshPending(value) {", "export function setAdminRefreshPending(value: boolean) {").replace("export function setAdminCoalesceHits(value) {", "export function setAdminCoalesceHits(value: number) {").replace("export function setScheduleAdminRefreshGlobal(fn) {", "export function setScheduleAdminRefreshGlobal(fn: () => void) {").replace("export function setWiredAdminListeners(value) {", "export function setWiredAdminListeners(value: boolean) {").replace("  window.GeolocationService = service;", "  (window as any).GeolocationService = service;"));
fix(B+"/registration/court/useClearCourtFlow.ts", c => c.replace("  const setSelectedCourtToClear = useCallback((value) => {", "  const setSelectedCourtToClear = useCallback((value: number | null) => {").replace("  const setClearCourtStep = useCallback((value) => {", "  const setClearCourtStep = useCallback((value: number) => {"));
fix(B+"/registration/court/useCourtAssignmentResult.ts", c => c.replace("  const setJustAssignedCourt = useCallback((value) => {", "  const setJustAssignedCourt = useCallback((value: number | null) => {").replace("  const setAssignedSessionId = useCallback((value) => {", "  const setAssignedSessionId = useCallback((value: string | null) => {").replace("  const setAssignedEndTime = useCallback((value) => {", "  const setAssignedEndTime = useCallback((value: string | null) => {").replace("  const setHasAssignedCourt = useCallback((value) => {", "  const setHasAssignedCourt = useCallback((value: boolean) => {"));

// Batch 2: Modals and router
fix(B+"/registration/modals/BlockWarningModal.tsx", c => c.replace("import React from 'react';", "import React from 'react';\ninterface BlockWarning {\n  type: string;\n  reason: string;\n  startTime: string;\n  minutesUntilBlock: number;\n  limitedDuration?: number;\n  originalDuration?: number;\n}\n\ninterface BlockWarningModalProps {\n  warning: BlockWarning | null;\n  onConfirm: () => void;\n  onCancel: () => void;\n}").replace("const BlockWarningModal = ({ warning, onConfirm, onCancel }) => {", "const BlockWarningModal = ({ warning, onConfirm, onCancel }: BlockWarningModalProps) => {").replace("  const formatTime = (isoString) => {", "  const formatTime = (isoString: string) => {"));
fix(B+"/registration/router/RegistrationRouter.tsx", c => c.replace("// @ts-check\n", "").replace("import React from 'react';", "import React from 'react';\nimport type { AppState, Handlers } from '../../types/appTypes';\n\ninterface RegistrationRouterProps {\n  app: AppState;\n  handlers: Handlers;\n}").replace("export default function RegistrationRouter({ app, handlers }) {", "export default function RegistrationRouter({ app, handlers }: RegistrationRouterProps) {"));

// Batch 3: Route components
const routeReplacements = (c, level) => c.replace("// @ts-check\n", "").replace("import React from 'react';", `import React from 'react';\nimport type { AppState, Handlers } from '${level}/types/appTypes';\n\ninterface ${c.includes("HomeRoute") ? "HomeRoute" : c.includes("GroupRoute") ? "GroupRoute" : c.includes("CourtRoute") ? "CourtRoute" : c.includes("AdminRoute") ? "AdminRoute" : c.includes("SuccessRoute") ? "SuccessRoute" : "ClearCourtRoute"}Props {\n  app: AppState;\n  handlers: Handlers;\n}`);
fix(B+"/registration/router/routes/HomeRoute.tsx", c => c.replace("// @ts-check\n", "").replace("import React from 'react';", "import React from 'react';\nimport type { AppState, Handlers } from '../../../types/appTypes';\n\ninterface HomeRouteProps {\n  app: AppState;\n  handlers: Handlers;\n}").replace("export function HomeRoute({ app, handlers }) {", "export function HomeRoute({ app, handlers }: HomeRouteProps) {"));
fix(B+"/registration/router/routes/GroupRoute.tsx", c => c.replace("// @ts-check\n", "").replace("import React from 'react';", "import React from 'react';\nimport type { AppState, Handlers } from '../../../types/appTypes';\n\ninterface GroupRouteProps {\n  app: AppState;\n  handlers: Handlers;\n}").replace("export function GroupRoute({ app, handlers }) {", "export function GroupRoute({ app, handlers }: GroupRouteProps) {"));
fix(B+"/registration/router/routes/CourtRoute.tsx", c => c.replace("// @ts-check\n", "").replace("import React from 'react';", "import React from 'react';\nimport type { AppState, Handlers } from '../../../types/appTypes';\n\ninterface CourtRouteProps {\n  app: AppState;\n  handlers: Handlers;\n}").replace("export function CourtRoute({ app, handlers }) {", "export function CourtRoute({ app, handlers }: CourtRouteProps) {"));
fix(B+"/registration/router/routes/AdminRoute.tsx", c => c.replace("// @ts-check\n", "").replace("import React from 'react';", "import React from 'react';\nimport type { AppState, Handlers } from '../../../types/appTypes';\n\ninterface AdminRouteProps {\n  app: AppState;\n  handlers: Handlers;\n}").replace("export function AdminRoute({ app, handlers }) {", "export function AdminRoute({ app, handlers }: AdminRouteProps) {"));
fix(B+"/registration/router/routes/SuccessRoute.tsx", c => c.replace("// @ts-check\n", "").replace("import React from 'react';", "import React from 'react';\nimport type { AppState, Handlers } from '../../../types/appTypes';\n\ninterface SuccessRouteProps {\n  app: AppState;\n  handlers: Handlers;\n}").replace("export function SuccessRoute({ app, handlers }) {", "export function SuccessRoute({ app, handlers }: SuccessRouteProps) {"));
fix(B+"/registration/router/routes/ClearCourtRoute.tsx", c => c.replace("// @ts-check\n", "").replace("import React from 'react';", "import React from 'react';\nimport type { AppState, Handlers } from '../../../types/appTypes';\n\ninterface ClearCourtRouteProps {\n  app: AppState;\n  handlers: Handlers;\n}").replace("export function ClearCourtRoute({ app, handlers }) {", "export function ClearCourtRoute({ app, handlers }: ClearCourtRouteProps) {"));

// Batch 4: QRScanner
fix(B+"/registration/components/QRScanner.tsx", c => c.replace("export function QRScanner({ onScan, onClose, onError }) {", "interface QRScannerProps {\n  onScan: (token: string) => void;\n  onClose?: () => void;\n  onError?: (err: unknown) => void;\n}\n\nexport function QRScanner({ onScan, onClose, onError }: QRScannerProps) {").replace("  const [, setIsScanning] = useState(false); // Getter unused, setter used\n  const [error, setError] = useState(null);\n  const [hasPermission, setHasPermission] = useState(null);\n  const scannerRef = useRef(null);\n  const containerRef = useRef(null);", "  const [, setIsScanning] = useState(false); // Getter unused, setter used\n  const [error, setError] = useState<string | null>(null);\n  const [hasPermission, setHasPermission] = useState<boolean | null>(null);\n  const scannerRef = useRef<InstanceType<typeof Html5Qrcode> | null>(null);\n  const containerRef = useRef<HTMLDivElement | null>(null);").replace("      if (err.message?.includes('Permission')) {", "      if ((err as Error).message?.includes('Permission')) {").replace("      } else if (err.message?.includes('NotFoundError')) {", "      } else if ((err as Error).message?.includes('NotFoundError')) {").replace("        setError(\`Failed to start camera: \${err.message}\`);", "        setError(\`Failed to start camera: \${(err as Error).message}\`);"));

// Batch 5: useMemberIdentity
fix(B+"/registration/memberIdentity/useMemberIdentity.ts", c => c.replace("import { useReducer, useCallback, useRef } from 'react';", "import { useReducer, useCallback, useRef } from 'react';\ninterface FrequentPartnerCacheEntry {\n  status: 'loading' | 'error' | 'ready';\n  ts: number;\n  data?: unknown[];\n}\n\ninterface MemberIdentityBackend {\n  queries: {\n    getFrequentPartners: (memberId: string) => Promise<{ ok: boolean; partners?: Array<{ member_id: string; display_name: string; member_number: string; play_count: number }> }>;  };\n}\n\ninterface MemberIdentityDeps {\n  backend: MemberIdentityBackend | null | undefined;\n}").replace("export function useMemberIdentity({ backend }) {", "export function useMemberIdentity({ backend }: MemberIdentityDeps) {").replace("  const frequentPartnersCacheRef = useRef({});", "  const frequentPartnersCacheRef = useRef<Record<string, FrequentPartnerCacheEntry>>({});").replace("  const setMemberNumber = useCallback((value) => {", "  const setMemberNumber = useCallback((value: string) => {").replace("  const setCurrentMemberId = useCallback((value) => {", "  const setCurrentMemberId = useCallback((value: string | null) => {").replace("  const setFrequentPartners = useCallback((value) => {", "  const setFrequentPartners = useCallback((value: unknown[]) => {").replace("  const setFrequentPartnersLoading = useCallback((value) => {", "  const setFrequentPartnersLoading = useCallback((value: boolean) => {").replace("    async (memberId) => {", "    async (memberId: string) => {").replace("          const partners = result.partners.map((p) => ({", "          const partners = result.partners!.map((p) => ({").replace("        setFrequentPartners(cached.data);", "        setFrequentPartners(cached.data || []);"));

console.log("done");
