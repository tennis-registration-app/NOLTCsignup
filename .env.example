# =============================================================================
# NOLTC Tennis Registration System - Environment Variables (names-only template)
# =============================================================================
# Copy to .env.local for local development.
# For Vercel deployment, set these in Project Settings → Environment Variables.
#
# See docs/ENVIRONMENT.md for detailed setup instructions.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (REQUIRED)
# -----------------------------------------------------------------------------
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# API base URL for Edge Functions (optional - derived from SUPABASE_URL if omitted)
# VITE_BASE_URL=

# -----------------------------------------------------------------------------
# Feature Flags (OPTIONAL)
# -----------------------------------------------------------------------------
# NOTE: Vite env vars are strings. Use "true" / "false".

# Default: true (set to "false" to disable)
# VITE_USE_REAL_AI=

# Default: true (set to "false" to disable)
# VITE_ENABLE_WET_COURTS=

# Default: false (set to "true" to enable)
# VITE_DEBUG_MODE=
