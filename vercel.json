{
  "buildCommand": "node scripts/check-env.js && npm run -s lint:ratchet && npm run -s type:ratchet && npm run -s coverage:ratchet && npm run -s test:fixtures && npm run build",
  "outputDirectory": "dist"
}
